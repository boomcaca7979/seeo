-- ===== SeeO 种子数据 =====
-- ⚠️ 执行前请先注册一个账号，然后把下面的 <YOUR_USER_ID> 替换为该账号的 ID
-- 获取方式：Supabase Dashboard → Authentication → Users → 复制 User ID
-- 或在 SQL Editor 执行：select id, email from auth.users;

-- 替换下面的 UUID 为你的真实用户 ID
-- insert into public.projects (user_id, name, domain) values
--   ('<YOUR_USER_ID>', '示例项目', 'example.com'),
--   ('<YOUR_USER_ID>', '电商演示', 'shop-demo.cn'),
--   ('<YOUR_USER_ID>', '博客测试', 'blog-test.com');

-- 取消上面注释并替换 <YOUR_USER_ID> 后执行
