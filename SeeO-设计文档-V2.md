# SeeO 品牌视觉设计文档 · V2

> 相比 V1（暗色底），本版将主色调从暗色系调整为**鲜艳的站酷黄底色**，整体基调从「深空科技」变为「阳光下的数据车间」——外壳明亮有活力，数据/产品层保留科技感的深色卡片，两者形成反差而不是互相稀释。

---

## 1. 设计基调一句话说明

**底色是品牌，卡片是产品。** 页面大面积使用高饱和黄色作为品牌识别面，所有实际承载数据的区域（导航栏、功能卡片、仪表盘、CTA 模块）用深色卡片"浮"在黄色画布上，形成"晒得到太阳、也看得清数据"的反差感，避免通篇黄色导致的廉价感，也避免深色卡片太多导致底色存在感被稀释。

---

## 2. 色彩系统

### 2.1 核心色板

| 角色 | 名称 | Hex | 说明 |
|---|---|---|---|
| 页面底色（主品牌色） | 站酷黄 Station Yellow | `#FFD400` | 整站画布背景色，鲜艳、高饱和、不做渐变处理，保持纯色的干脆感 |
| 页面底色（备用/分区） | 站酷黄-深 | `#F5C400` | 用于和主黄色区分出的次级分区背景（如页脚前的过渡区），避免长页面通篇同一个黄无层次 |
| 深色卡片面（产品层） | 墨黑 Ink | `#14121A` | 导航栏、功能卡片、仪表盘面板、页脚的背景色，与黄色形成强对比 |
| 深色卡片-次级 | 墨黑-浅 Ink Elevated | `#1D1B26` | 深色卡片内部再分层时使用（如卡片里的子模块） |
| 品牌强调色（用于深色卡片上） | 亮金 Bright Gold | `#FFD400` | 与底色同色，但仅用于深色卡片内的按钮/高亮文字/图表主色，形成"黄色从底色跳到卡片上"的呼应 |
| 科技渐变（仅限深色卡片内使用） | 极光渐变 Aurora | `#6C4CFF → #2F8CFF → #FFD400` | 用于深色卡片内的信号图形、CTA 按钮底、图表强调线，绝不作为大面积页面背景 |
| 数据-正向 | 深青 Teal | 深色卡片上 `#21D19F` ／ 黄色底上 `#0E7C5E` | 排名上升、外链增长等正向指标 |
| 数据-负向 | 珊瑚红 Coral | 深色卡片上 `#FF5C5C` ／ 黄色底上 `#B23B34` | 排名下降、问题告警等负向指标 |

### 2.2 文字色（按所在背景区分——这是本版最容易出错的地方，务必按此表执行）

| 所在背景 | 主文字 | 次要文字 | 弱化文字/占位符 |
|---|---|---|---|
| 站酷黄底色上 | 墨黑 `#14121A` | 深橄榄棕 `#6B5A00` | `#8A7A2E` |
| 深色卡片上 | 米白 `#F5F6FA` | 灰蓝 `#9AA3B8` | `#6B7280` |

> 关键原则：**黄色底色上永远不出现浅灰色文字**——很多设计师会习惯性用中性灰做次要文字，但中性灰在高饱和黄色上对比度会显得"脏"，次要文字必须用从黄色本身加深出来的橄榄棕色系，才会显得是同一套调性，而不是随手贴上去的。

### 2.3 使用比例建议

- 站酷黄底色：页面可视面积占比约 55%-65%（画布本身）
- 深色卡片：占比约 30%-40%（导航、功能区块、仪表盘、CTA）
- 极光渐变：仅作为强调，占比不超过 5%，且只出现在深色卡片内部

---

## 3. 字体系统（沿用 V1，未改动）

| 角色 | 字体 | 使用场景 |
|---|---|---|
| 标题 / 展示字体 | Space Grotesk（西文）+ 系统黑体（中文，PingFang SC / 微软雅黑加粗） | 主标题、板块大标题、数据大数字 |
| 正文字体 | Inter（西文）+ 系统黑体常规 | 段落说明文字、按钮文字、导航 |
| 数据/等宽字体 | JetBrains Mono | 所有指标数字、KD 分数、排名变化、实时数据条、标签徽章——凡是"数据感"的文字都用这个字体，形成统一的"这是真实工具数据"的视觉暗示 |

字重：全站只用 **常规 / 中粗 / 加粗** 三档，不引入额外字重层级，保持克制。

---

## 4. 版式与布局原则

1. **黄色画布 + 深色浮层卡片**，卡片一律使用 16px 圆角、无投影（用卡片自身的深色边界和背景色差来分层，而不是加阴影，阴影在高饱和背景上会显脏）。
2. **首屏（Hero）**：黄色画布 + 一块居中的深色"面板"承载核心可视化（关键词引力场图），面板本身像一块"嵌入画布里的屏幕"，这样图里的渐变发光节点才有足够对比度可读。
3. **实时数据条（Ticker）**：改为深色底横条，贯穿页面宽度，与上下的黄色画布形成一条"深色分割线"，同时保证等宽字体的数据在深色背景上足够清晰。
4. **功能卡片区**：黄色画布上排列深色卡片（3 栏），卡片内用小标签（挖词/追踪/诊断）而非编号，标签色使用黄色系（在深色卡片上使用亮金色）。
5. **仪表盘预览区**：深色卡片矩阵，卡片内图表线条/柱状用极光渐变或亮金色，是全站"科技感"最集中的区域。
6. **CTA 板块**：深色卡片打底，按钮使用站酷黄填充，形成"从黄色画布里长出来的按钮"的呼应感。
7. **页脚**：深色，与导航栏首尾呼应，把黄色画布"框"在中间。

一句话总结版式逻辑：**黄色不是装饰，是画布本身；深色不是主角，是数据和科技感的容器。**

---

## 5. 组件规范速览

| 组件 | 背景 | 文字/图形色 | 说明 |
|---|---|---|---|
| 顶部导航栏 | 墨黑 `#14121A` | 米白文字 + 亮金 CTA 按钮 | 固定深色，不随页面滚动变色 |
| 主 CTA 按钮（黄底上） | 墨黑填充 | 米白文字 | 黄底上的按钮用深色，形成反色块 |
| 主 CTA 按钮（深色卡片上） | 站酷黄填充 | 墨黑文字 | 深色卡片上的按钮用黄色，形成呼应 |
| 次级按钮 | 透明 + 深色描边 | 墨黑文字（黄底上）/ 米白文字（深色卡片上） | |
| 标签 / Badge | 半透明黄或半透明深色 | 对应场景文字色 | 用于"挖词/追踪/诊断"等分类标签 |
| 数据徽章（上升/下降） | 深色卡片上的半透明色块 | 深青 / 珊瑚红 | 只在深色卡片内出现，黄底上不直接摆放此类小色块，避免色彩打架 |

---

## 6. 动效原则（沿用 V1）

- 首屏核心可视化保留缓慢的"呼吸"发光动效（3s 一循环）和卫星节点的轻微漂浮动效，作为全站唯一的"信号动效"。
- 实时数据条保持匀速横向滚动。
- 其余区域不做多余动效，避免"AI 生成感"里常见的到处加动画的问题。

---

## 7. 文案基调（沿用 V1，未改动）

- 中文标题善用"看"字呼应 SeeO 品牌名（看清 / 看懂 / 看到）。
- 全站用主动语态、说人话，不用"赋能""闭环""抓手"类词汇。
- 按钮文字动词开头，说清点了会发生什么（"开始分析"而不是"提交"）。

---

## 8. 待确认事项

1. 站酷黄 `#FFD400` 在移动端小屏 + 强光环境下的可读性建议实际打样确认一次（高饱和黄在部分手机屏幕上会有轻微溢色）。
2. 深色卡片在黄色画布上大面积使用时，建议做一版实际截图在不同显示器上比对，确认对比度是否刺眼（尤其是长时间使用的仪表盘页面，可以考虑仪表盘内页整体切换为"深色卡片为主、黄色仅作点缀"的反转版式，而营销首页保持黄色为主）。
3. 需要设计一版深色模式下的备用配色（部分企业用户习惯深色系工具），本文档暂未覆盖。
